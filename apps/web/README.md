# @hyperframe-editor/web

Next.js 15 editor app. Placeholder until Day 6 of the PLAN.md week-1 checklist.

The Day-6 build will wire:

- a single editor route showing chat + preview,
- SSE-stream from the worker into the chat panel,
- a Render button that posts to `/api/render`.

See [`../../PLAN.md`](../../PLAN.md) §6 (editor UI spec) and §14 (week-1 day-by-day).
